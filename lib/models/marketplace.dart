

import 'package:market_place/models/card.dart';

/*class Market {
  String? id;
  UserCard user_card;
  int? price;

  Market({
    this.id,
    required this.user_card,
    this.price,
  });

  factory Market.fromJson(Map<String, dynamic> json) {
    return Market(
      id: json['_id'],
      user_card: json['user_card'] != null ? UserCard.fromJson(json['user_card']) : UserCard(card: MyCard()),
      price: json['price'],
    );
  }

  Map<String, dynamic> toJson() {
    return {
      '_id': id,
      'user_card': user_card.toJson(),
      'price': price,
    };
  }
}

*/